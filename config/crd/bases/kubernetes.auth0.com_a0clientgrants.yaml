---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: a0clientgrants.kubernetes.auth0.com
spec:
  group: kubernetes.auth0.com
  names:
    kind: A0ClientGrant
    listKind: A0ClientGrantList
    plural: a0clientgrants
    shortNames:
    - a0cgr
    singular: a0clientgrant
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: A0ClientGrant is the Schema for the a0clientgrants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A0ClientGrantSpec defines the desired state of A0ClientGrant
            properties:
              conf:
                description: Conf specifies the desired configuration for the client
                  grant
                properties:
                  audience:
                    description: Audience is a reference to the A0ResourceServer (audience)
                    properties:
                      identifier:
                        description: Identifier is the identifier of the resource
                          server
                        type: string
                      scopes:
                        description: Scopes are the scopes associated with this resource
                          server
                        items:
                          type: string
                        type: array
                    type: object
                  clientRef:
                    description: ClientRef is a reference to the A0Client
                    properties:
                      id:
                        description: Id is the Auth0 ID of the client
                        type: string
                      name:
                        description: Name is the name of the referenced client
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referenced client.
                          If empty, the same namespace as the referencing resource is assumed.
                        type: string
                    type: object
                  scope:
                    description: Scope lists the scopes granted to the client
                    items:
                      type: string
                    type: array
                type: object
              init:
                description: Init specifies the initial configuration when creating
                  a new client grant
                properties:
                  audience:
                    description: Audience is a reference to the A0ResourceServer (audience)
                    properties:
                      identifier:
                        description: Identifier is the identifier of the resource
                          server
                        type: string
                      scopes:
                        description: Scopes are the scopes associated with this resource
                          server
                        items:
                          type: string
                        type: array
                    type: object
                  clientRef:
                    description: ClientRef is a reference to the A0Client
                    properties:
                      id:
                        description: Id is the Auth0 ID of the client
                        type: string
                      name:
                        description: Name is the name of the referenced client
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referenced client.
                          If empty, the same namespace as the referencing resource is assumed.
                        type: string
                    type: object
                  scope:
                    description: Scope lists the scopes granted to the client
                    items:
                      type: string
                    type: array
                type: object
              policy:
                description: Policy defines the allowed operations for this client
                  grant
                items:
                  description: V1EntityPolicyType defines the policy types for Auth0
                    entities
                  enum:
                  - Create
                  - Update
                  - Delete
                  type: string
                type: array
              tenantRef:
                description: TenantRef is a reference to the A0Tenant this client
                  grant belongs to
                properties:
                  name:
                    description: Name is the name of the referenced tenant
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the referenced tenant.
                      If empty, the same namespace as the referencing resource is assumed.
                    type: string
                required:
                - name
                type: object
            required:
            - conf
            - tenantRef
            type: object
          status:
            description: A0ClientGrantStatus defines the observed state of A0ClientGrant
            properties:
              id:
                description: Id is the Auth0 client grant ID
                type: string
              lastConf:
                description: LastConf contains the last applied configuration
                type: object
                x-kubernetes-preserve-unknown-fields: true
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
